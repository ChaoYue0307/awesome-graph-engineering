name: Quality

on:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: "23 4 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docs:
    name: Docs and data quality gates
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.x"

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"

      - name: Check Markdown formatting
        uses: DavidAnson/markdownlint-cli2-action@v24
        with:
          globs: "**/*.md"

      - name: Run repository quality gates
        run: bash scripts/check.sh

  sources:
    name: Source status audit
    # These reach third-party APIs, so they run on the weekly schedule and on
    # demand rather than on every pull request.
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.x"

      - name: Check listed repositories for archival, renames, and inactivity
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: python3 scripts/check_repos.py

      - name: Check listed arXiv papers for undisclosed withdrawals
        run: python3 scripts/check_arxiv.py

  links:
    name: Link audit
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Restore lychee cache
        uses: actions/cache@v6
        with:
          path: .lycheecache
          key: lychee-${{ github.sha }}
          restore-keys: lychee-

      - name: Check links
        uses: lycheeverse/lychee-action@v2
        with:
          args: >-
            --config lychee.toml
            --cache
            --max-cache-age 1d
            README.md DEFINITION.md METHODOLOGY.md TAXONOMY.md COMPARISON.md
            ANTI-PATTERNS.md CONTRIBUTING.md BRAND.md LAUNCH-KIT.md
            i18n/README.md i18n/*/README.md data/README.md huggingface/README.md
            docs/llms.txt
          fail: true
